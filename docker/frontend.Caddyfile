{
	auto_https off
	admin off
}

:3000 {
	@apidocs path /docs /openapi.json

	handle @apidocs {
		uri strip_suffix /
		reverse_proxy {$API_URL}
	}

	handle {
		uri strip_suffix /
		reverse_proxy http://media-manager-frontend:3001
	}
}
